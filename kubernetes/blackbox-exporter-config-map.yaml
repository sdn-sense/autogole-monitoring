apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-blackbox-exporter-config-map
  labels:
    app: prometheus-blackbox-exporter
data:
  blackbox.yaml: |
    modules:

      v4_nrm_cenic:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_cenic.token
      v6_nrm_cenic:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_cenic.token

      v4_nrm_cenicdev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_cenicdev.token
      v6_nrm_cenicdev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_cenicdev.token

      v4_nrm_cenic_la:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_cenic_la.token
      v6_nrm_cenic_la:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_cenic_la.token

      v4_nrm_gpn:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_gpn.token
      v6_nrm_gpn:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-nrm_gpn.token

      v4_t1_us_fnal:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t1_us_fnal.token
      v6_t1_us_fnal:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t1_us_fnal.token

      v4_t2_us_caltech:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_caltech.token
      v6_t2_us_caltech:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_caltech.token

      v4_t2_us_caltech_dev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_caltech_dev.token
      v6_t2_us_caltech_dev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_caltech_dev.token

      v4_t2_us_caltech_test:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_caltech_test.token
      v6_t2_us_caltech_test:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_caltech_test.token

      v4_t2_us_mghpcc:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_mghpcc.token
      v6_t2_us_mghpcc:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_mghpcc.token

      v4_t2_us_nebraska:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_nebraska.token
      v6_t2_us_nebraska:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_nebraska.token

      v4_t2_us_sdsc:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc.token
      v6_t2_us_sdsc:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc.token

      v4_t2_us_sdsc_dev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_dev.token
      v6_t2_us_sdsc_dev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_dev.token

      v4_t2_us_sdsc_oasis:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_oasis.token
      v6_t2_us_sdsc_oasis:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_oasis.token

      v4_t2_us_sdsc_oasisdev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_oasisdev.token
      v6_t2_us_sdsc_oasisdev:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_oasisdev.token

      v4_t2_us_sdsc_s63:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_s63.token
      v6_t2_us_sdsc_s63:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_sdsc_s63.token

      v4_t2_us_ucsd:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_ucsd.token
      v6_t2_us_ucsd:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_ucsd.token

      v4_t2_us_uchicago:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_uchicago.token
      v6_t2_us_uchicago:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_uchicago.token

      v4_t2_us_uh_gorex:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_uh_gorex.token
      v6_t2_us_uh_gorex:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_uh_gorex.token

      v4_t2_us_wisconsin:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_wisconsin.token
      v6_t2_us_wisconsin:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t2_us_wisconsin.token

      v4_t3_fabric_amst:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_amst.token
      v6_t3_fabric_amst:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_amst.token

      v4_t3_fabric_cern:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_cern.token
      v6_t3_fabric_cern:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_cern.token

      v4_t3_fabric_cern1:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_cern1.token
      v6_t3_fabric_cern1:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_cern1.token

      v4_t3_fabric_star:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_star.token
      v6_t3_fabric_star:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_fabric_star.token

      v4_t3_us_fab_bluefield3:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_bluefield3.token
      v6_t3_us_fab_bluefield3:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_bluefield3.token

      v4_t3_us_fab_losa:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_losa.token
      v6_t3_us_fab_losa:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_losa.token

      v4_t3_us_fab_newy:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_newy.token
      v6_t3_us_fab_newy:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_newy.token

      v4_t3_us_fab_ucsd:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_ucsd.token
      v6_t3_us_fab_ucsd:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_fab_ucsd.token

      v4_t3_us_internet2:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_internet2.token
      v6_t3_us_internet2:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_internet2.token

      v4_t3_us_nrp:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_nrp.token
      v6_t3_us_nrp:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_nrp.token

      v4_t3_us_sl_400g:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_sl_400g.token
      v6_t3_us_sl_400g:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_sl_400g.token

      v4_t3_us_starlight:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_starlight.token
      v6_t3_us_starlight:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          authorization:
            credentials_file: /etc/oidc/oidc-t3_us_starlight.token

      https_v4_network_2xx:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip4"
          tls_config:
            insecure_skip_verify: true
            cert_file: /etc/tls/tls.crt
            key_file: /etc/tls/tls.key
      https_v6_network_2xx:
        prober: http
        http:
          fail_if_ssl: false
          fail_if_not_ssl: true
          preferred_ip_protocol: "ip6"
          tls_config:
            insecure_skip_verify: true
            cert_file: /etc/tls/tls.crt
            key_file: /etc/tls/tls.key
      http_v4_network_2xx:
        prober: http
        http:
          fail_if_ssl: true
          fail_if_not_ssl: false
          preferred_ip_protocol: "ip4"
      http_v6_network_2xx:
        prober: http
        http:
          fail_if_ssl: true
          fail_if_not_ssl: false
          preferred_ip_protocol: "ip6"
      icmp_v4:
        prober: icmp
        icmp:
          preferred_ip_protocol: "ip4"
      icmp_v6:
        prober: icmp
        icmp:
          preferred_ip_protocol: "ip6"
